version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: personatech-frontend
    ports:
      - "2077:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
